name: flutter_weather_dashboard
description: Flutter port of the Weather Dashboard UI (React) with OpenWeatherMap and AQICN integration.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  geolocator: ^14.0.2
  intl: ^0.20.2
  fl_chart: ^0.65.0
  flutter_dotenv: ^6.0.0
  # 🔥 Firebase packages
  firebase_core: 4.2.1
  firebase_auth: ^6.1.1
  firebase_database: ^12.0.4
  firebase_messaging: ^16.0.3
  cloud_firestore: 6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
